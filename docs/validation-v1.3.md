# 1.3.0 验证记录

日期：2026-09-25。只使用公开仓库代码与虚构测试材料，不包含真实附件和客户数据。

## 执行与安装

- macOS：临时技能目录实际安装，shared 与两个 Word 技能入口分别能在取消 NODE_PATH 后构建。
- 工程回归覆盖安全目录、重复渲染、字体环境透传、转换与提取失败、空来源、
  模块整张缺失、同义表头、短材料三问、安装冲突/幂等/依赖失败，以及虚构样例算术。
- 最终本地回归 25 项通过；样例实际渲染 9 页，摘要第 3–4 页共 2 页，已目视检查中文、图表、表格及分页。
- 五个技能经 Skill Creator quick_validate 验证；验证器所需 PyYAML 仅装在临时 venv，不修改系统 Python。
- 五个 .skill 与五份单文件方法包均可生成；共享文件同步检查通过。
- 同一输入两次构建的 word/*.xml 一致。DOCX ZIP 自身可能因时间戳而字节不同，不承诺二进制逐字节一致。
- 源码图表未由 selftest 重画；自检输出只在临时目录。
- 初次目视发现 macOS 内置 LibreOffice 正文中文缺失，随后修复仅作用于子进程的字体配置并实际复测。
- 发现旧摘要定位会把移动后的目录当正文，已加入目录位置变化和标题匹配的回归；不将零页当通过。
- 文字检查的“是不是稳赚”命中来自问题与否定回答，人工按上下文判为合理风险解释，未当作推荐承诺。

GitHub Linux CI 与发布状态以仓库 Actions / Releases 的实际记录为准，不以本文代替远端运行证据。
Windows、Claude Code、真实同事安装和跨环境版式完全一致尚未验证。

## 独立模拟使用（不是作者自评）

测试模型沿用本次运行模型；全新代理仅获得技能、原始材料与任务，不给预期答案。
原始回复留在本地任务记录，公共仓库仅存不含敏感信息的结果摘要。

| 场景 | 实际观察 |
|---|---|
| 新任务，范围不确定，指定原句 | 新版推荐局部销售工具，给产业位置、替代品选择、客户组合三个不同方向及取舍，保留原句，停止等待确认 |
| 确认后只要一页口径＋三问 | 按范围完成，中文、对话交付，未扩成 Word、KPI 或三语；使用相对权重差，区分管理费与总成本 |
| 数据稀缺的短债创意 | 新会话保留原句但不推荐本金/按月支付主张，给不同方向与需补证据，没有套用电力或 AI 故事，等待确认 |
| 旧版同输入对照 | 旧版也遵守先确认并保留原句，确认缩小范围后也完成三问；新版首轮更明确区分叙事路线及各自证据需求 |

本轮没有证据证明新版本在所有任务都优于旧版，也没有测量成交转化或同事成功率。
改进结论限定为：流程与保留机制明确化、观察到的模拟行为符合需求、已发现工程问题有回归覆盖。

## 未完成的证据

- 未把用户内部成品当原始资料重新做金融研究；没有完整原始材料包，不能声称金标准复现。
- 未外部核验虚构材料，不把它们作为真实产品建议。
- 尚需真实同事用实际获准材料试用，检查理解、时间成本与最终可用性。
- 内容语义检查不能完全脚本化，仍保留研究、复算与人工审阅。

## 回滚

基线标签 backup/pre-marketing-upgrade-20260925 与 v1.2.0 均指向 4175329。
主分支通过新增提交升级，不重写历史；本机恢复步骤见 README。
